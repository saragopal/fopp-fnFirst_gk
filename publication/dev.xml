<!-- Basically the same as runestone.ptx, but host="web" seems to be necessary for ActiveCode to
work locally -->
<publication>

    <!-- Uncomment the following to remove pagination (one HTML file per chapter) -->
    <!-- <common>
        <chunking level="1" />
    </common> -->

    <!-- directories are relative to the main source PreTeXt file -->
    <source>
        <directories external="Figures" generated="GenFigs" />
    </source>

    <html>
        <platform host="web" />
        <!-- knowled checkpoints interfere with ActiveCode problems -->
        <knowl exercise-inline="no" example="no" listing="yes" />
    </html>
</publication>